# 🚂 夢想探索號列車 — 前端遊戲規格書（spec.md）

---

## 一、專案概述

**遊戲名稱**：夢想探索號列車  
**類型**：多人實體派對遊戲（主持人操作投影大螢幕）  
**格數**：16 格線性冒險地圖  
**目標**：最先抵達第 16 格「夢夢車站」的隊伍獲勝  
**操作者**：主持人（單一裝置控制，投影給所有人看）  
**玩家人數**：2～6 隊（隊伍數可在遊戲前設定）

---

## 二、技術規格

| 項目 | 說明 |
|------|------|
| 框架 | React（單頁應用） |
| 樣式 | Tailwind CSS + 自訂 CSS 動畫 |
| 狀態管理 | React `useState` / `useReducer`（純前端，無後端） |
| 螢幕尺寸 | 主要目標：1920×1080（全螢幕投影） |
| 字型 | 中文主字型：Noto Sans TC；展示標題：可搭配裝飾性英文字型 |
| 音效 | 可選：使用 Web Audio API 或 `<audio>` 標籤播放音效 |

---

## 三、頁面結構

### 3.1 設定畫面（Setup Screen）

進入遊戲前的初始化頁面。

**功能：**
- 輸入各隊伍名稱（2～6 隊）
- 選擇每隊代表顏色或圖示（🚂 列車圖示，不同顏色區分）
- 按下「出發！」按鈕進入主遊戲畫面

---

### 3.2 主遊戲畫面（Main Game Screen）

**版面配置（左右分割）：**

```
┌─────────────────────────────────────────────────────┐
│  [遊戲標題 Banner]                                   │
├────────────────────────┬────────────────────────────┤
│                        │                            │
│   地圖區（16 格軌道）   │    控制面板 & 資訊區        │
│                        │                            │
│  格子排列：蛇形或直線   │  - 當前回合隊伍             │
│  顯示各隊棋子位置       │  - 擲骰子按鈕              │
│                        │  - 骰子動畫                │
│                        │  - 格子說明提示             │
│                        │  - 隊伍狀態列表             │
└────────────────────────┴────────────────────────────┘
```

---

## 四、地圖格子規格

共 16 格，每格有以下資料結構：

```typescript
type CellType =
  | "start"         // 起點
  | "end"           // 終點
  | "boost"         // 加速（多擲一次）
  | "supply_pack"   // 夢想補給包（隨機前進 1/2/3 格）
  | "station"       // 補給站（完成站長指令）
  | "surprise"      // 探險驚喜包（隨機事件）
  | "obstacle"      // 障礙（後退 N 格）
  | "rest"          // 休息一次（跳過下一輪）
  | "repair"        // 維修站（無特殊動作）
  | "goto"          // 傳送到指定格

interface Cell {
  id: number;           // 格號 1～16
  name: string;         // 格子名稱
  type: CellType;
  description: string;  // 顯示給玩家看的說明文字
  effect: CellEffect;   // 觸發的遊戲邏輯
  icon: string;         // Emoji 或圖示
  color: string;        // 格子背景色（Tailwind class 或 hex）
}
```

### 格子對照表

| 格號 | 名稱 | 類型 | 效果 |
|------|------|------|------|
| 1 | 思麥特車站（起點） | `start` | 無 |
| 2 | 燃料加滿加速前進 | `boost` | 多擲骰子一次 |
| 3 | 夢想補給包 | `supply_pack` | 隨機前進 1/2/3 格 |
| 4 | 艾特迪車站 | `station` | 補給站（無特殊動作） |
| 5 | 探險驚喜包 | `surprise` | 50% 回起點 / 50% 前往第 9 格 |
| 6 | 軌道落石擋路 | `obstacle` | 後退 2 格 |
| 7 | 列車維修 | `rest` | 跳過下一輪 |
| 8 | 探險驚喜包 | `surprise` | 50% 回起點 / 50% 前往第 9 格 |
| 9 | 史貝瑞特車站 | `station` | 站長指令：做出頭上愛心動作，拍照 |
| 10 | 夢想補給包 | `supply_pack` | 隨機前進 1/2/3 格 |
| 11 | 雷瑟邦伯車站 | `repair` | 維修站（無動作） |
| 12 | 莫洛車站 | `station` | 站長指令：全隊踏地板發出聲音 |
| 13 | 遭遇狂風大雨 | `obstacle` | 後退 2 格 |
| 14 | 夢想補給包 | `supply_pack` | 隨機前進 1/2/3 格 |
| 15 | 列車故障 | `goto` | 傳送到第 11 格（雷瑟邦伯車站） |
| 16 | 夢夢車站（終點） | `end` | 抵達即獲勝！ |

---

## 五、遊戲流程（Game Loop）

```
1. 確認當前回合隊伍
2. 主持人按下「擲骰子」
   → 播放骰子動畫（旋轉 + 數字閃爍）
   → 顯示點數（1～6）
3. 棋子動畫移動到新格子
4. 觸發格子效果：
   ├─ supply_pack → 顯示抽籤動畫，隨機前進 1/2/3 格
   ├─ surprise    → 顯示驚喜包動畫，50% 兩種結果之一
   ├─ boost       → 顯示提示，再擲一次骰子
   ├─ obstacle    → 顯示障礙動畫，後退 N 格
   ├─ rest        → 標記該隊「休息中」，下一輪跳過
   ├─ goto        → 棋子傳送動畫，移至目標格
   └─ station     → 顯示站長指令全螢幕提示（主持人操作）
5. 檢查是否抵達第 16 格 → 若是，播放勝利動畫
6. 切換到下一隊伍（跳過「休息中」的隊伍）
7. 重複
```

---

## 六、UI 元件清單

### 6.1 地圖（GameBoard）
- 16 格排列（建議蛇形或直線軌道，火車風格）
- 每格顯示：格號、格名、圖示、背景色
- 各隊棋子（小圖示）疊加顯示在格子上
- 目前回合隊伍的格子高亮提示

### 6.2 骰子元件（DiceRoller）
- 按鈕觸發擲骰
- 3D 翻滾動畫（CSS `transform: rotateX/Y`）
- 動畫結束後顯示最終點數（大字）

### 6.3 格子效果彈窗（EventModal）
- 全螢幕或半螢幕覆蓋層
- 顯示觸發的事件說明（大字、圖示、動畫）
- 「確認」按鈕關閉，繼續遊戲

### 6.4 隊伍狀態列（TeamStatusPanel）
- 列出所有隊伍：名稱、目前格號、狀態（正常 / 休息中）
- 當前回合高亮標示

### 6.5 站長指令覆蓋（MissionOverlay）
- 專為第 9、12 格設計的全螢幕指令提示
- 顯示指令說明 + 動畫倒數（可選）
- 主持人按下「完成」後繼續

### 6.6 勝利畫面（WinScreen）
- 全螢幕慶祝動畫（彩帶、煙火特效）
- 顯示獲勝隊伍名稱
- 「再玩一次」按鈕重設遊戲

---

## 七、特殊互動說明

### 夢想補給包（格 3、10、14）
- 主持人帶領全場喊：「123，打開夢想補給包！」
- 前端播放開箱動畫
- 隨機抽出前進 1、2 或 3 格
- 大字顯示結果

### 探險驚喜包（格 5、8）
- 播放神秘動畫（轉輪 or 抽籤效果）
- 結果 A（50%）：返回第 1 格，顯示「回到起點囉 ^_^」
- 結果 B（50%）：傳送到第 9 格（史貝瑞特車站）

### 站長指令（格 9、12）
- 格 9：全螢幕顯示「做出頭上大愛心動作！📸」提示，主持人拍照
- 格 12：顯示「全隊一起腳踏地板！」提示

---

## 八、音效規劃（可選）

| 事件 | 音效建議 |
|------|---------|
| 擲骰子 | 骰子滾動聲 |
| 棋子移動 | 火車行進聲（短） |
| 夢想補給包 | 開箱音效 |
| 障礙格 | 錯誤提示音 |
| 勝利 | 歡呼 + 彩帶音效 |
| 站長指令 | 哨音或喇叭聲 |

---

## 九、視覺風格建議

| 項目 | 建議 |
|------|------|
| 主色調 | 深藍夜空 + 橘黃燈光（火車旅行氛圍） |
| 字型 | 標題：圓潤可愛風；內文：清晰易讀 |
| 格子風格 | 木質質感或卡片感，帶陰影 |
| 動畫風格 | 活潑、快速、不過度複雜 |
| 棋子圖示 | 各隊不同顏色的小火車 🚂 |
| 整體基調 | 歡樂、派對感、適合全年齡 |

---

## 十、後續擴充建議

- [ ] 支援自訂格子內容（主持人可編輯格子說明）
- [ ] 加入計時器（限時完成站長指令）
- [ ] 支援行動裝置版（各隊自己手機操作）
- [ ] 儲存遊戲記錄（LocalStorage）
- [ ] 多語系支援（中/英）